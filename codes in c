Write a C program to print the powers of 2 table for the power 0 to 10, both positive and negative.
#include <stdio.h>

int main()
{
    int n;
    int p=1;
    float q;
    for(n=0;n<11;n++)
    {
        if(n==0)
        {
            p=1;
        }
        else
        {
            p=p*2;
            //q=1.0/(double) p;
            //printf("\n%d    %d    %f",n,p,q);
        }
        q=1.0/(double) p;
        printf("\n%d    %d    %f",n,p,q);
    } 
    
    printf("\n");
    return 0;
}
